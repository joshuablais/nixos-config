#+title: Readme

This is where you will add secrets using agenix to encrypt.

** How to use
1. Declare -secret- in secrets.nix.

2. Create your -secret-.age file, save the .age file and it will be automatically encrypted.

3. Secret is now encrypted per recipient and decrypts at runtime.

4. Ship to repo, colmena apply or rebuild machine with required secret.
